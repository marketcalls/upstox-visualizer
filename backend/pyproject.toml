[project]
name = "upstox-chart-console"
version = "1.0.0"
description = "Upstox login, API setup and 5-minute TradingView charts on FastAPI + SQLite"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "httpx>=0.27.0",
    "pydantic>=2.9.0",
]

[tool.uv]
package = false
